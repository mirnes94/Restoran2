<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lakj.comspace.simpletextclient"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".Menu" />
        <activity android:name=".CheckOrder" />
        <activity android:name=".BurgerMenu" />
        <activity android:name=".PizzaMenu" />
        <activity
            android:name=".SlimpleTextClientActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".FoodLab"
            android:label="Food Lab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Order_Type" />
        <activity
            android:name=".finalize_order"
            android:label="@string/title_activity_finalize_order" />
        <activity
            android:name=".thankyou"
            android:label="@string/title_activity_thankyou" />
    </application>

</manifest>